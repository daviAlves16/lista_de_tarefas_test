import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles(() => ({
  typographyMenu: {
    paddingRight: '0.5rem', 
    paddingLeft: '0.5rem'
  },
  buttonSingIn: {
    marginRight: '1rem !important'
  },
  boxIconCircle: {
    display: 'flex', 
    alignItems: 'center'
  }
}));